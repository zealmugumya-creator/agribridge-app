{
  "name": "agribridge-app",
  "version": "1.0.0",
  "description": "AgriBridge Uganda - Farm-to-Table Platform",
  "main": "index.js",
  "scripts": {
    "sync": "cap sync android",
    "build:android": "cap sync android && cd android && ./gradlew assembleDebug",
    "build:release": "cap sync android && cd android && ./gradlew assembleRelease",
    "open:android": "cap open android",
    "update": "npx cap update android"
  },
  "keywords": ["agribridge", "uganda", "farming", "agritech"],
  "author": "Zeal Mugumya <hello@agribridge.ug>",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
